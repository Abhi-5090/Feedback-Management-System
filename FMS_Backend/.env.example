# ── Server ────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development

# ── Database ──────────────────────────────────────────────────────────────
# For MongoDB transactions to work, Mongo must run as a replica set.
# Local single-node replica set example: mongodb://127.0.0.1:27017/fms?replicaSet=rs0
# Plain standalone also works — the API degrades gracefully to ordered writes
# guarded by the DeviceLock unique index when transactions aren't supported.
MONGO_URI=mongodb://127.0.0.1:27017/fms

# ── Auth / crypto ─────────────────────────────────────────────────────────
# REQUIRED in production: with NODE_ENV=production the server REFUSES to boot if
# these are missing, shorter than 16 chars, or contain a placeholder like
# "change_me". Generate strong values: `openssl rand -hex 32`.
JWT_SECRET=change_me_to_a_long_random_secret
JWT_EXPIRES_IN=7d
# Salt mixed into device-signature hashes (Layer 2 anti-duplicate). Keep secret & stable.
DEVICE_SALT=change_me_to_another_long_random_secret

# Whether auth/device cookies get the `Secure` flag. Defaults to on when
# NODE_ENV=production. Set to "false" ONLY for a plain-HTTP demo, where a Secure
# cookie would be silently dropped by the browser.
# COOKIE_SECURE=true

# ── CORS ──────────────────────────────────────────────────────────────────
# Comma-separated allowlist of frontend origins.
CLIENT_ORIGINS=http://localhost:5173

# ── App identity ──────────────────────────────────────────────────────────
# Shown in email subjects/branding, and used to build links inside emails.
APP_NAME=Feedback Management
# Public URL of the SPA. Defaults to the first CLIENT_ORIGINS entry.
APP_URL=http://localhost:5173

# ── Email (welcome mail + password reset) ─────────────────────────────────
# LEAVE BLANK IN DEVELOPMENT: with no SMTP host the mailer prints each message
# — including the reset link — to the server console, so the whole flow works
# with zero setup. Fill these in for production.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Feedback Management <no-reply@example.com>

# How long a password-reset link stays valid (minutes).
RESET_TOKEN_MINUTES=30

# ── Seed admin (used by seed.js) ──────────────────────────────────────────
SEED_ADMIN_EMAIL=admin@example.com
# Leave blank to auto-generate a strong password (printed once on seed).
SEED_ADMIN_PASSWORD=